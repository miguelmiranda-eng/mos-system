/* TEMA ÚNICO (rediseño 2026-07): se retiró el switch claro/oscuro — el sistema
   usa una sola paleta azulada (ver index.css). El componente se conserva como
   no-op para no romper a sus importadores (CEODashboard). */
const ThemeSwitch = () => null;

export default ThemeSwitch;
